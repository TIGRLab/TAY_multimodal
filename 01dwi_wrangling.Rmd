---
title: "01dwi_wrangling"
author: "Anisha Miah"
date: "2025-06-04"
output: html_document
---
https://github.com/ggseg/ggsegGlasser
```{r setup}
# Enable this universe
#options(repos = c(
    #ggseg = 'https://ggseg.r-universe.dev',
    #CRAN = 'https://cloud.r-project.org'))

# Install some packages
#install.packages('ggsegGlasser')

#install.packages("remotes")
# install.packages("remotes")
#remotes::install_github("ggseg/ggsegExtra")

library(tidyverse)
library(ggseg)
library(ggplot2)
library(ggsegGlasser)
library(ggsegSchaefer)
```
/archive/data/TAY/pipelines/versioned_outputs/pet_subsample/
## R Markdown
```{r}
#example from Dr. Erin
sub_CMH00000041_ses_05_desc_aparcaseg_model_noddi_results <- read_delim("/mnt/tigrlab/archive/data-2.0/TAY/pipelines/versioned_outputs/pet_subsample/ciftify_parcellations/sub-CMH00000041/ses-05/dwi/sub-CMH00000041_ses-05_desc-aparcaseg_model-noddi_results.tsv", 
delim = "\t", escape_double = FALSE, trim_ws = TRUE)
```

test try with noddi results data
```{r}
files_list = list.files(path ="/archive/data/TAY/pipelines/versioned_outputs/pet_subsample/ciftify_parcellations/", pattern="desc-aparcaseg_model-noddi_results.tsv", recursive = TRUE, full.names=TRUE)
```
```{r}
#want to use read_delim file like earlier to concatenate
# using anonymous function (~) for own interest
#desc_aparcaseg_model_noddi_results = files_list %>% map_dfr(~ read_delim(.x, delim = "\t", escape_double = FALSE, trim_ws = TRUE))

desc_aparcaseg_model_noddi_results = map_dfr(files_list, function(x) 
    {read_delim (x, delim = "\t", escape_double = FALSE, trim_ws = TRUE, show_col_types=FALSE)}) #show_col_types to quiet message
```
Function used to read through many patient files and concatenate into a dataframe
commented out output path option currently, may make an if statement if separate reusable .tsv files are needed later
```{r}
wrangle_data = function (path,pattern #, output_path
                         ) {
  files_list = list.files(path = path, pattern=pattern, recursive = TRUE, full.names=TRUE)

  results_file = map_dfr(files_list, function(x)
  {read_delim (x, delim = "\t", show_col_types = FALSE, na=c("NA", "", "n/a")) %>% 
    mutate(subject = str_extract(x, pattern = "sub-[^/\\\\_]+")) %>% 
    mutate(task = str_extract(x, pattern = "nback|rest")) %>%
    mutate (session = str_extract(x, pattern = "ses-0\\d")) %>%
    select(subject, task, session, everything())
  })
  #write_tsv(results_file, output_path)
}
```

concatenate three different parcellations for alff, reho, coverage and motion
```{r}
alff_xcpd_4S156Parcel_results=wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/", "4S156Parcels_stat-alff_bold.tsv")

alff_xcpd_HCP_results=wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/", "HCP_stat-alff_bold.tsv")

alff_xcpd_Glasser_results=wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/", "Glasser_stat-alff_bold.tsv")
```
```{r}
reHo_xcpd_4S156Parcels_results = wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/","4S156Parcels_stat-reho_bold.tsv")

reHo_xcpd_HCP_results = wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/","HCP_stat-reho_bold.tsv")

reHo_xcpd_Glasser_results = wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/","Glasser_stat-reho_bold.tsv")
```

```{r}
coverage_xcpd_HCP_results= wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/","HCP_stat-coverage_bold.tsv")

coverage_xcpd_Glasser_results= wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/","Glasser_stat-coverage_bold.tsv")

coverage_xcpd_4S156_results= wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/","4S156Parcels_stat-coverage_bold.tsv")
```
```{r}
motion_xcpd_results = wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/","_desc-linc_qc.tsv")
```

checking through motion and coverage files for outlier subjects
```{r}
#xcpd paper given mentions if coverage below 0.5 = bad parcellation 
coverage_Glasser_below_threshold =  filter(coverage_xcpd_Glasser_results, coverage <0.5) #435
coverage_HCP_below_threshold = filter(coverage_xcpd_HCP_results, coverage<0.5) #0
coverage_4S156_belowthreshold = filter(coverage_xcpd_4S156_results, coverage<0.5) #4 observations, same subject 846 for Cerebellar_Region4 

#motion QC - thresholding if higher than 0.5
motion_above_threshold = filter(motion_xcpd_results, mean_fd>0.5) #2 - sub 148 for two scans
```

function for global corr calculation:
```{r}
calculate_GC = function(dataframe){
  #timeseries datasets are in wide format, need to change format to long
  long_ts = dataframe %>% pivot_longer(cols = -c(subject,task,session), names_to = "Node", values_to="Value")

  calculate = long_ts %>% group_by(subject,task,session, Node) %>% 
    summarise(
     mean_GC = mean(Value), 
      Pos_GC = mean(subset(Value, Value>0)),
      Abs_GC = mean(abs(Value))
      )
}
```
```{r}
ts_HCP_concatenate = wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/", "HCP_stat-mean_timeseries.tsv") 
ts_HCP_rest = filter(ts_HCP_concatenate, task =="rest")

GC_HCP= calculate_GC(ts_HCP_rest)
```
```{r}
ts_Glasser_concatenate = wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/", "Glasser_stat-mean_timeseries.tsv") 
ts_Glasser_rest = filter(ts_Glasser_concatenate, task =="rest")

GC_Glasser= calculate_GC(ts_Glasser_rest)
```

making a brain map
```{r}
#randomly select ten subjects and make brainmap of it (will need ggseg)
glasser_df = as_tibble(glasser)
```
```{r}
glasser_df
```


```{r}
ten_sub = sample(unique(GC_Glasser$subject), 10)
GC_plot = filter(GC_Glasser, subject %in% ten_sub) %>% rename(label = Node) %>%    mutate(subject = as.factor(subject)) %>% 
  mutate(label = str_replace(label, "Left", "lh_L") %>% 
           str_replace("Right", "rh_R"))
```

```{r}

#to note: glasser label doesn't have lh_L_10pp 
#for loop going through ten subjects doesnt make a pretty picture :( only makes awful ugly tibbles that all say lh_L_10pp doesn't exist
#for (i in 1:3){
ggseg(.data = GC_plot %>% filter(subject == ten_sub[1]),
      atlas = glasser,
      mapping = aes(fill=mean_GC)) +
  scale_fill_viridis_c() +
  theme_void() +
  labs(title = paste("Mean GSCorr for Patient", ten_sub[1]))
#}
```
ATTEMPT FOR GLOBAL CORR WITH ONE PATIENT:
need to do Global Corr calculations
- concatenate parcellated timeseries

path for example timeseries: /scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/sub-CMH00000023/ses-03/func/sub-CMH00000023_ses-03_task-rest_run-02_space-fsLR_seg-HCP_stat-mean_timeseries.tsv

https://builtin.com/data-science/grouping-r 
```{r}
#global corr calculator
#hint given: to calculate - grad correlation matrix
#Total GC = mean of row
#Positive GC = mean of all positive values in the row
#Absolute GC = mean of absolute values in the row

#let's use an example file first with one patient, note this patient only has resting state
ts_practice = wrangle_data("/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/", "sub-CMH00000023_ses-03_task-rest_run-02_space-fsLR_seg-HCP_stat-mean_timeseries.tsv") 

#issue with this is that the parcellations are columns instead of rows - i'm assuming we are calcualting each parcellation instead of averaging each point in time for timeseries... what would you even analyze with each point in time...
#mean of each column, then in new file put each column as an item in a row? so that one row has subject, task, parcel, total GC, +GC, absolute GC

#change parcels so its all in one column
long_ts = ts_practice %>% pivot_longer(cols = -c(subject,task,session), names_to = "Node", values_to="Value")

calculate = long_ts %>% group_by(subject,task,session, Node) %>% 
  summarise(
    mean_GC = mean(Value), 
    Pos_GC = mean(subset(Value, Value>0)),
    Abs_GC = mean(abs(Value))
      )
```



This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
