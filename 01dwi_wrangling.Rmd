---
title: "01dwi_wrangling"
author: "Anisha Miah"
date: "2025-06-04"
output: html_document
---
https://github.com/ggseg/ggsegGlasser
```{r setup}
# Enable this universe
options(repos = c(
    ggseg = 'https://ggseg.r-universe.dev',
    CRAN = 'https://cloud.r-project.org'))
# Install some packages
install.packages('ggsegGlasser')

# Enable this universe
options(repos = c(
    ggseg = 'https://ggseg.r-universe.dev',
    CRAN = 'https://cloud.r-project.org'))

# Install some packages
install.packages('ggsegSchaefer')

#install.packages("remotes")
# install.packages("remotes")
#remotes::install_github("ggseg/ggsegExtra")

library(tidyverse)
library(ggseg)
library(ggplot2)
library(ggsegGlasser)
library(ggsegSchaefer)
```

Function used to read through many patient files and concatenate into a dataframe
commented out output path option currently, may make an if statement if separate reusable .tsv files are needed later
```{r}
wrangle_data = function (path,pattern #, output_path
                         ) {
  files_list = list.files(path = path, pattern=pattern, recursive = TRUE, full.names=TRUE)

  results_file = map_dfr(files_list, function(x)
  {read_delim (x, delim = "\t", show_col_types = FALSE, na=c("NA", "", "n/a")) %>% 
    mutate(subject = str_extract(x, pattern = "sub-[^/\\\\_]+")) %>% 
    mutate(task = str_extract(x, pattern = "nback|rest")) %>%
    mutate (session = str_extract(x, pattern = "ses-0\\d")) %>%
    select(subject, task, session, everything())
  })
  #comment out filter if both rest and nback wanted
  #filter(results_file, task=="rest")
  #write_tsv(results_file, output_path)
}

path_xcpd = "/scratch/amiah/SCanD_project/data/local/derivatives/xcp_d/0.7.3/"
path_noddi = "/archive/data/TAY/pipelines/versioned_outputs/pet_subsample/ciftify_parcellations/"
```

concatenate three different parcellations for alff, reho, coverage and motion
```{r}
alff_xcpd_4S156Parcel_results=wrangle_data(path_xcpd, "4S156Parcels_stat-alff_bold.tsv")

alff_xcpd_HCP_results=wrangle_data(path_xcpd, "HCP_stat-alff_bold.tsv")

alff_xcpd_Glasser_results=wrangle_data(path_xcpd, "Glasser_stat-alff_bold.tsv")
```
```{r}
reHo_xcpd_4S156Parcels_results = wrangle_data(path_xcpd,"4S156Parcels_stat-reho_bold.tsv")

reHo_xcpd_HCP_results = wrangle_data(path_xcpd,"HCP_stat-reho_bold.tsv")

reHo_xcpd_Glasser_results = wrangle_data(path_xcpd,"Glasser_stat-reho_bold.tsv")
```

```{r}
coverage_xcpd_HCP_results= wrangle_data(path_xcpd,"HCP_stat-coverage_bold.tsv")

coverage_xcpd_Glasser_results= wrangle_data(path_xcpd,"Glasser_stat-coverage_bold.tsv")

coverage_xcpd_4S156_results= wrangle_data(path_xcpd,"4S156Parcels_stat-coverage_bold.tsv")
```
```{r}
motion_xcpd_results = wrangle_data(path_xcpd,"_desc-linc_qc.tsv")
```

checking through motion and coverage files for outlier subjects
```{r}
#xcpd paper given mentions if coverage below 0.5 = bad parcellation 
coverage_Glasser_below_threshold =  filter(coverage_xcpd_Glasser_results, coverage <0.5) #435
coverage_HCP_below_threshold = filter(coverage_xcpd_HCP_results, coverage<0.5) #0
coverage_4S156_belowthreshold = filter(coverage_xcpd_4S156_results, coverage<0.5) #4 observations, same subject 846 for Cerebellar_Region4 

#motion QC - thresholding if higher than 0.5
motion_above_threshold = filter(motion_xcpd_results, mean_fd>0.5) #2 - sub 148 for two scans
```

function for global corr calculation:
```{r}
calculate_GC = function(dataframe, string){
  #timeseries datasets are in wide format, need to change format to long
  long_ts = dataframe %>% pivot_longer(cols = -c(subject,task,session), names_to = "Node", values_to="Value")
  
  if(string == "individual"){
    group = long_ts %>% group_by(subject, task, session, Node)
  } else if (string =="group"){
    group = long_ts%>% group_by(Node) 
  } else{
    print("Please specify group or individual analysis")
  }

  calculate =  group %>% 
    summarise(
     mean_GC = mean(Value), 
      Pos_GC = mean(subset(Value, Value>0)),
      Abs_GC = mean(abs(Value))
      )
}
```
```{r}
ts_HCP_concatenate = wrangle_data(path_xcpd, "HCP_stat-mean_timeseries.tsv") 
ts_HCP_rest = filter(ts_HCP_concatenate, task =="rest")

GC_HCP= calculate_GC(ts_HCP_rest) 
```
Schaefer brainmap of mean gscorr
```{r}
ts_4S156Parcels_concatenate = wrangle_data(path_xcpd, "4S156Parcels_stat-mean_timeseries.tsv") 
ts_4S156Parcels_rest = filter(ts_4S156Parcels_concatenate, task =="rest")
```

```{r}
GC_4S156Parcels_group = calculate_GC(ts_4S156Parcels_rest, "group") %>% 
  rename(region = Node) #%>%    
 # mutate(region = str_replace(region, "Left", "lh_L") %>% 
          # str_replace("Right", "rh_R"))

```
```{r}
schaefer_df = as_tibble(schaefer17_100)
```
```{r}
#i'm confused on which scahefer atlas to use
ggseg(.data = GC_4S156Parcels_group,
      atlas = schaefer17_100,
      mapping = aes(fill=mean_GC)) +
  scale_fill_viridis_c() +
  theme_void() +
  labs(title = paste("Mean GSCorr for Group"))
```


Glasser brainmap of mean gscorr
```{r}
glasser_df = as_tibble(glasser)
ts_Glasser_concatenate = wrangle_data(path_xcpd, "Glasser_stat-mean_timeseries.tsv") 
ts_Glasser_rest = filter(ts_Glasser_concatenate, task =="rest") 
```
```{r}
#randomly select ten subjects and make brai nmap of it (will need ggseg)
ten_sub = sample(unique(ts_Glasser_rest$subject), 10)
GC_Glasser_rand_individual= calculate_GC(ts_Glasser_rest, "individual") %>% 
  filter(subject %in% ten_sub) %>%
  rename(label = Node) %>%    
  mutate(subject = as.factor(subject)) %>% 
  mutate(label = str_replace(label, "Left", "lh_L") %>% 
           str_replace("Right", "rh_R"))

GC_Glasser_individual= calculate_GC(ts_Glasser_rest, "individual") %>% 
  rename(label = Node) %>%    
  mutate(subject = as.factor(subject)) %>% 
  mutate(label = str_replace(label, "Left", "lh_L") %>% 
           str_replace("Right", "rh_R"))
GC_Glasser_group = calculate_GC(ts_Glasser_rest, "group") %>% 
  rename(label = Node) %>%    
  mutate(label = str_replace(label, "Left", "lh_L") %>% 
           str_replace("Right", "rh_R"))
```

making individual brain map for Glasser for ten subjects
```{r}
#to note: glasser label doesn't have lh_L_10pp, so a tibble pops up that shows it's empty 
ggseg(.data = GC_Glasser_rand_individual,
      atlas = glasser,
      mapping = aes(fill=mean_GC)) +
  facet_wrap(~subject) +
  scale_fill_viridis_c() +
  theme_void() +
  labs(title = paste("Mean GSCorr for Subject", ~subject))
```
```{r}
ggseg(.data = GC_Glasser_group,
      atlas = glasser,
      mapping = aes(fill=mean_GC)) +
  scale_fill_viridis_c() +
  theme_void() +
  labs(title = paste("Mean GSCorr for Group"))
```
```{r}
#making a correlation matrix to QC 
install.packages("ggcorrplot")
library(ggcorrplot)
```
```{r}
GC_Glasser_wide = subset(GC_Glasser_individual, select=c(subject, label, mean_GC)) %>% pivot_wider(names_from = subject, values_from = mean_GC)
GC_Glasser_matrix = cor(GC_Glasser_wide %>% select(-label), use = "pairwise.complete.obs")
```
```{r}
ggcorrplot(GC_Glasser_matrix, hc.order = TRUE, type = "lower") +
  theme (axis.text.x = element_text(angle= 90, vjust=1, hjust = 1, size=8),
         axis.text.y = element_text(size=8))

# a lot of white space visible. should i filter for sessions? it's already currently filtered by task
```
---------------------------------------------------------------

QSIPREP 

---------------------------------------------------------------
```{r}
noddi_Glasser_results = wrangle_data(path_noddi, "Glasser_model-noddi_results.tsv") %>% subset(select = -c(task))
noddi_Scahefer7N100_results = wrangle_data(path_noddi, "Schaefer7N100_model-noddi_results.tsv") %>% subset(select = -c(task))
```
```{r}
#look at coverage files
noddi_Glasser_coverage = filter(noddi_results, coverage<0.5)
```


This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
